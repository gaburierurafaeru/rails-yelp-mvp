<div class="container">
  <h1>Restaurants</h1>
  <div class="cards">
  </div>
  <ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>
   <p><%= link_to 'New Restaurant', new_restaurant_path(@restaurant), class: 'btn btn-light'  %></p>
</div>
